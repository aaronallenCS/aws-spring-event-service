# -----------------------------
# Default active profile
# -----------------------------
spring.profiles.active=local

# -----------------------------
# Actuator exposure
# -----------------------------
management.endpoints.web.exposure.include=health,info,metrics

# -----------------------------
# App metadata (used by /version)
# -----------------------------
app.name=event-service
app.version=0.1.0